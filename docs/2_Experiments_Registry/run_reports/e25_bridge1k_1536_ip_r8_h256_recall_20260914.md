# E25 bridge_1k seq=1536 recall_single r=8 remainder-off H=256 SSMax log — exclusive MATCH wall (rung 5bg)

**Date:** 2026-09-14
**Machine:** Odra 1× RTX 3090 (GPU 0). Polonez left empty. E22 Byobu sessions untouched.
**Run ID:** `e25_1536_ip_r8_h256_recall` (800; floor — do not extra-step)
**WandB:** n/a (probe; no `compute/*` audit — there is no W&B run)
**Raw JSON/plots:** `/opt/cursor/artifacts/e25_bridge1k_1536_ip_r8_h256_recall/`
**Raw log:** `/opt/cursor/artifacts/e25_bridge1k_1536_ip_r8_h256_recall/probe.log`
**Best checkpoint:** none (on-the-fly rows; models discarded)
**Git commit:** `c90bc24` (Odra E24 worktree; E22 main checkout untouched)
**Git tag:** —
**Related:** 1024 MATCH H=256 log PASS [`e25_bridge1k_ip_r8_h256_recall_20260914.md`](e25_bridge1k_ip_r8_h256_recall_20260914.md) · 1536 INDEX chance (E18 ~0) [`e25_bridge1k_1536_ip_r16_mean_far_copy_20260914.md`](e25_bridge1k_1536_ip_r16_mean_far_copy_20260914.md)

---

## Goal

Locked: 1024 packed MATCH (`recall_single`) inplace r=8 rem-off **H=256
SSMax log S1 PASS (60.35 bits @8k)**; INDEX at 1536 already **S1 FAIL (0
bits @800)** for both E18 and E21. This rung maps the MATCH length wall
after 1024 S1 PASS: same 1024-winning compressor, `--scale bridge_1k
--seq_len 1536` (probe override; no new scale). Window/gap stay `16 < 64`
(right-align gap 65). QUERY leftover **r=8 residue 2** (same as 1024
MATCH). Packed answer 32 / 64-bit prize. `--global_layers` stays **1**.

E18 may live (unlike INDEX@1536). Score vs 0.75× **live E18**; if E18
were ~0, score vs 0.75× dense. Recalibrate dense in this JSON. Climbing
at 800 → extra-step 8k; **floor → do not extra-step**. Do **not** pass
`--message_pool_remainder`. Do not 16k. Do not retune the compressor.

## Configuration

| Item | Value |
|---|---|
| Family | `perceiver_ar`: dense / e18 / e21 / e18_local |
| Width | **H=256** · 2.261M (e21 2.277M) · kv=1 · **`logit_scale=log`** · stack=2 |
| E21 | query boundary id 10, **r=8**, remainder **off**, **`msg_override=real`**, **`msg_inplace=True`**, **`msg_rawkv=False`**, **`msg_idslots=True`**, pack_stride 0, **`glob_layers=1`** |
| Data | `--scale bridge_1k --seq_len 1536`, gap=64, window=16, packed answer 32 / **64-bit** prize |
| Placement | `--evidence_align right` (row gap 65/65/65; QUERY 1498 leftover **2**) |
| Init | `--warm_residuals` |
| Device | CUDA bf16 (`--amp auto`), `backend=sdpa` · **batch 32** (~3.7GB, no OOM) |

```
bash scripts/e24_bapo_hunt.sh e25_1536_ip_r8_h256_recall 0 \
  --scale bridge_1k --seq_len 1536 --recipe recall_single --arch dense e18 e21 e18_local \
  --evidence_align right --warm_residuals --hidden 256 --global_logit_scale log \
  --amp auto --message_ratio 8 --message_slots_inplace --message_identity_slots \
  --steps 800 --k1_mult 4
# --seq_len 1536 required (bridge_1k default is 1024)
# no --message_pool_remainder
# no --global_layers (default 1)
# dense first (underscore --no-dense_first unused)
# floor at 800: do not extra-step
```

Byobu `E25_1536_r8_h256_recall`. Log:
`seq=1536  gap=64  window=16  prize=64.00 bits  answer_len=32  row_gap[min/med/max]=65/65/65`
`logit_scale=log  msg_r=8  msg_remainder=False  msg_inplace=True  msg_rawkv=False  msg_idslots=True  glob_layers=1`.

Dense first. Hunt JSON: `hidden: 256`, `global_logit_scale: log`,
`global_layers: 1`, `message_ratio: 8`, `message_pool_remainder: false`,
`message_identity_slots: true`, `message_inplace_raw_kv: false`,
`batch: 32`, `seq_len: 1536`, `scale: bridge_1k`, `calibrated: true`.
Hunt exit **0**. Params <100M. **No new scale enum.**

## Training Outcome

| arch | acc | step | recovered bits | flow | B/tok |
|---|---|---|---|---|---|
| dense | **99.9%** | 500 | **63.65** | 0.995 | 0.005180 |
| e18 | **85.8%** (best **86.1%** @500) | 800 | **52.58** | 0.822 | 0.004279 |
| **e21 @800** | **24.7%** | 800 | **0.00** | **0.000** | **0** |
| e18_local | 24.7% | 800 | 0.00 | 0.000 | 0 |

Dense left chance through 100, **59.8% @200**, **86.9% @250**, early-stop
**99.9% @500**. E18 chance through 300, click **50.9% @350**, **85.1%
@450**, plateau **85.8% / 52.58 bits** @800 (**live — not ~0**). E21
chance every eval (CE at ln(4); **0 bits**; best 27.5% @100/600). Not
climbing.

E18 is **live**. S1 bar is **0.75× E18 = 39.44 bits** (also vs **0.75×
dense = 47.74 bits**). E21 has **0**. **8k not run.**

## Gates vs this rung

| gate | result |
|---|---|
| **S0** | **PASS.** Dense 99.9% / 63.65 bits @500. |
| **S1 vs 0.75× live E18** | **FAIL.** 0 ≪ 39.44 bits. Chance floor. Do not pass via 0.75×0 — E18 is live. |
| **content vs 0.75× dense** | **FAIL.** 0 ≪ 47.74 bits. |
| **S2** plots | **PASS.** |
| **K1** | not triggered |
| **K2** | **PASS.** `e18_local` 24.7% / 0 bits. |
| **K3** | floor at 800 (flow 0). |

Do **not** extra-step to 8k (floor). Do not 16k. Do not remainder-on. Do
not retune r / H / logit_scale. Do not hops. Do not Glyph. Do not unfreeze
`u`/`delta`. Do not 2048 this turn.

## Interpretation

**Exclusive compressed MATCH that passed at 1024 is chance at 1536** on
the same H=256 SSMax-log r=8 rem-off identity-mean compressor **and the
same leftover residue 2**. Dense still copies 64 bits in 500 steps, so
the instrument is live (not K1). E18 still recovers **52.58 bits** at
800 — this is **not** the shared one-read length wall that killed INDEX at
1536 (E18 ~0 there). Do **not** relabel E18's 52.58 bits as E21.

The MATCH length wall for this recipe sits in **(1024 PASS, 1536
FAIL]**. Unlike INDEX, E18 is alive; the exclusive slot channel is the
thing that died. Stacking compressor knobs is a different experiment.

## Decision

Keep the spec in `ahead/`. Next ONE: **parent decides** (do not run
here). Exclusive MATCH wall is **(1024 PASS, 1536 FAIL]**. Do not stack
knobs on this compressor. Do not 8k. Do not 16k. Do not 2048. Do not
hops seq=268. INDEX extra-steps stay stopped. Hops extra-steps stay
stopped. SELECT extra-steps stay stopped. Not Glyph. Do not unfreeze
`u`/`delta`.

*Related: `master_experiment_log.md`, `docs/experiments_specs/done_success/E25_e21_bapo_capability_ladder.md`, `agenda.md`*
