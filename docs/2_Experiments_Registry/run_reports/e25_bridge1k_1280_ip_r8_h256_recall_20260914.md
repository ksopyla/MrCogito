# E25 bridge_1k seq=1280 recall_single r=8 remainder-off H=256 SSMax log — exclusive MATCH wall (rung 5bh)

**Date:** 2026-09-14
**Machine:** Odra 1× RTX 3090 (GPU 0). Polonez left empty. E22 Byobu sessions untouched.
**Run ID:** `e25_1280_ip_r8_h256_recall` (800; floor — do not extra-step)
**WandB:** n/a (probe; no `compute/*` audit — there is no W&B run)
**Raw JSON/plots:** `/opt/cursor/artifacts/e25_bridge1k_1280_ip_r8_h256_recall/`
**Raw log:** `/opt/cursor/artifacts/e25_bridge1k_1280_ip_r8_h256_recall/probe.log`
**Best checkpoint:** none (on-the-fly rows; models discarded)
**Git commit:** `34c23e4` (Odra E24 worktree; E22 main checkout untouched)
**Git tag:** —
**Related:** 1024 MATCH H=256 log PASS [`e25_bridge1k_ip_r8_h256_recall_20260914.md`](e25_bridge1k_ip_r8_h256_recall_20260914.md) · 1536 MATCH exclusive FAIL [`e25_bridge1k_1536_ip_r8_h256_recall_20260914.md`](e25_bridge1k_1536_ip_r8_h256_recall_20260914.md)

---

## Goal

Locked: 1024 packed MATCH (`recall_single`) inplace r=8 rem-off **H=256
SSMax log S1 PASS (60.35 bits @8k)**; 1536 MATCH on the same compressor
**S1 FAIL (0 bits @800)** with live E18 **52.58 bits**. This rung tightens
the exclusive MATCH wall **(1024 PASS, 1536 FAIL]** by 256 tokens:
`--scale bridge_1k --seq_len 1280` (probe override; no new scale). Same
compressor. Window/gap stay `16 < 64` (right-align gap 65). QUERY leftover
**r=8 residue 2** (QUERY **1242**; same residue as 1024/1536 MATCH). Packed
answer 32 / 64-bit prize. `--global_layers` stays **1**. Remainder **off**.

E18 is live at 1536, so 1280 is a valid exclusive-length tighten (INDEX
1280 was forbidden while that wall was shared with dead E18). Score vs
0.75× **live E18**; if E18 were ~0, score vs 0.75× dense. Recalibrate dense
in this JSON. Climbing at 800 → extra-step 8k only; **floor → do not
extra-step**. Do **not** pass `--message_pool_remainder`. Do not 16k. Do
not retune the compressor.

## Configuration

| Item | Value |
|---|---|
| Family | `perceiver_ar`: dense / e18 / e21 / e18_local |
| Width | **H=256** · 2.261M (e21 2.277M) · kv=1 · **`logit_scale=log`** · stack=2 |
| E21 | query boundary id 10, **r=8**, remainder **off**, **`msg_override=real`**, **`msg_inplace=True`**, **`msg_rawkv=False`**, **`msg_idslots=True`**, pack_stride 0, **`glob_layers=1`** |
| Data | `--scale bridge_1k --seq_len 1280`, gap=64, window=16, packed answer 32 / **64-bit** prize |
| Placement | `--evidence_align right` (row gap 65/65/65; QUERY 1242 leftover **2**) |
| Init | `--warm_residuals` |
| Device | CUDA bf16 (`--amp auto`), `backend=sdpa` · **batch 32** (~3.2GB, no OOM) |

```
bash scripts/e24_bapo_hunt.sh e25_1280_ip_r8_h256_recall 0 \
  --scale bridge_1k --seq_len 1280 --recipe recall_single --arch dense e18 e21 e18_local \
  --evidence_align right --warm_residuals --hidden 256 --global_logit_scale log \
  --amp auto --message_ratio 8 --message_slots_inplace --message_identity_slots \
  --steps 800 --k1_mult 4
# --seq_len 1280 required (bridge_1k default is 1024)
# no --message_pool_remainder
# no --global_layers (default 1)
# dense first (underscore --no-dense_first unused)
# floor at 800: do not extra-step
```

Byobu `E25_1280_r8_h256_recall`. Log:
`seq=1280  gap=64  window=16  prize=64.00 bits  answer_len=32  row_gap[min/med/max]=65/65/65`
`logit_scale=log  msg_r=8  msg_remainder=False  msg_inplace=True  msg_rawkv=False  msg_idslots=True  glob_layers=1`.

Dense first. Hunt JSON: `hidden: 256`, `global_logit_scale: log`,
`global_layers: 1`, `message_ratio: 8`, `message_pool_remainder: false`,
`message_identity_slots: true`, `message_inplace_raw_kv: false`,
`batch: 32`, `seq_len: 1280`, `scale: bridge_1k`, `calibrated: true`.
Hunt exit **0**. Params <100M. **No new scale enum.**

## Training Outcome

| arch | acc | step | recovered bits | flow | B/tok |
|---|---|---|---|---|---|
| dense | **99.9%** | 550 | **63.85** | 0.998 | 0.006235 |
| e18 | **100%** | 400 | **63.95** | 0.999 | 0.006245 |
| **e21 @800** | **22.8%** (best **26.6%** @400) | 800 | **0.00** | **0.000** | **0** |
| e18_local | 25.0% | 800 | 0.00 | 0.000 | 0 |

Dense left chance through 250, **48.5% @300**, **73.8% @350**, **83.5%
@500**, early-stop **99.9% @550**. E18 chance through 300, click **67.7%
@350**, early-stop **100% / 63.95 bits** @400 (**live — not ~0**; stronger
than 1536's 52.58). E21 chance every eval (CE at ln(4); **0 bits**; best
26.6% @400). Not climbing.

E18 is **live**. S1 bar is **0.75× E18 = 47.96 bits** (also vs **0.75×
dense = 47.89 bits**). E21 has **0**. **8k not run.**

## Gates vs this rung

| gate | result |
|---|---|
| **S0** | **PASS.** Dense 99.9% / 63.85 bits @550. |
| **S1 vs 0.75× live E18** | **FAIL.** 0 ≪ 47.96 bits. Chance floor. Do not pass via 0.75×0 — E18 is live. |
| **content vs 0.75× dense** | **FAIL.** 0 ≪ 47.89 bits. |
| **S2** plots | **PASS.** |
| **K1** | not triggered |
| **K2** | **PASS.** `e18_local` 25.0% / 0 bits. |
| **K3** | floor at 800 (flow 0). |

Do **not** extra-step to 8k (floor). Do not 16k. Do not remainder-on. Do
not retune r / H / logit_scale. Do not hops. Do not Glyph. Do not unfreeze
`u`/`delta`. Do not MATCH 2048. Do not 1152/1408.

## Interpretation

**Exclusive compressed MATCH that passed at 1024 is chance at 1280** on
the same H=256 SSMax-log r=8 rem-off identity-mean compressor **and the
same leftover residue 2**. Dense still copies 64 bits in 550 steps, so
the instrument is live (not K1). E18 recovers **63.95 bits** at 400 —
full copy, stronger than 1536's 52.58. This is **not** the shared
one-read length wall that killed INDEX at 1536 (E18 ~0 there). Do
**not** relabel E18's 63.95 bits as E21.

The exclusive MATCH length wall for this recipe tightens to **(1024
PASS, 1280 FAIL]**. E18 is alive at 1280 and at 1536; the exclusive slot
channel is the thing that died. Stop further MATCH length extra-steps
after this 256-token tighten. Stacking compressor knobs (e.g. r=1
identity at 1280) is a different experiment.

## Decision

Keep the spec in `ahead/`. Next ONE: **STOP further MATCH length
extra-steps** (do not 1152/1408). Parent decides architecture (e.g. r=1
identity at the failing length) after this result. Do not stack
compressor knobs here. Do not 8k. Do not 16k. Do not MATCH 2048. Do not
hops seq=268. INDEX extra-steps stay stopped. Hops extra-steps stay
stopped. SELECT extra-steps stay stopped. Not Glyph. Do not unfreeze
`u`/`delta`.

*Related: `master_experiment_log.md`, `docs/experiments_specs/done_success/E25_e21_bapo_capability_ladder.md`, `agenda.md`*
